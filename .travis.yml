language: php
php:
- 5.3
- 5.6
env:
- DB=mysql
before_script:
- if [[ "$DB" == "mysql" ]]; then mysql -e "create database IF NOT EXISTS myapp;" -uroot; fi

before_script: 
- php /MyApp/scripts/Q/configure.php
- php /MyApp/scripts/Q/install.php --all

script: phpunit -c /platform/tests/q.phpunit.xml