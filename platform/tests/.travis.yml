language: php
php:
- 5.3
- 5.6
env:
- DB=mysql
before_script:
- if [[ "$DB" == "mysql" ]]; then mysql -e "create database IF NOT EXISTS myapp;" -uroot; fi

script: phpunit -c /projects/qbix/Q/platform/tests/q.phpunit.xml